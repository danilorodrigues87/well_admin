# Arquivos movidos

Os certificados foram copiados para:

```
storage/inter/certificado.crt
storage/inter/chave.key
```

Mantenha os originais aqui apenas como backup local, se desejar. **Não commitar** certificados no Git.

Configure `INTER_CLIENT_ID` e `INTER_CLIENT_SECRET` no `.env` — ver `docs/INTER.md`.
