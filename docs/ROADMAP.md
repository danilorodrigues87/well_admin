# Roadmap — Fase 2 (após painel web)

Itens adiados — não bloqueiam Fase 1.

1. **API REST** `/api/v1` — JWT, apps FlutterFlow (coletor em andamento — ver `docs/API.md`)
2. **SINIR** — envio automático de MTR
3. **Banco** — boleto + PIX + webhooks (Gateway pattern)
4. **Portal web cliente** — opcional se app FlutterFlow cobrir

Services criados na Fase 1 devem ser reutilizados pela API sem reescrita.
